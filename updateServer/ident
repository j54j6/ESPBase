UpdateTable:
	- MAC Adress (String) -
	- firmwareType (String) - 
	- firmwareVersion (String) - 
	- lastUpdated (long) - 
	- lastUpdateCheck (long) - 
	- deviceState: bool (true = Okay, false = broken) - 
	- locked: bool (if locked updates will not granted) - 
	- destroy: (if destroy true - not working firmware (or destroying firmware) will deployed during update procedure
	- isDestroyed: bool (for above)
	- updateToken: String (Auth)
	- updatePassword: String (Auth)


create table updates (device_id UINT NOT NULL AUTO_INCREMENT PRIMARY KEY, 
mac_address VARCHAR(12) NOT NULL, 
device_update_token VARCHAR(100) NOT NULL, 
device_update_password VARCHAR(100) NOT NULL, 
device_firmware_type VARCHAR(100) DEFAULT 'not_set', 
device_firmware_version VARCHAR(100) DEFAULT '0.0', 
last_updated DATE, last_update_check DATE, 
device_state TINYINT DEFAULT '1', 
device_is_locked TINYINT DEFAULT '0', 
device_to_destroy TINYINT DEFAULT '0', 
device_is_destroyed TINYINT DEFAULT '0')


